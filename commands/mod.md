---
description: "Retired alias — use /crew:sys (system-architect)"
argument-hint: <task>
---

This alias was retired in the catalog consolidation (25 → 17 roles). module-extension-architect was absorbed by system-architect: extension contracts are architecture decisions.

Tell the user in one line: `MOD → use /crew:sys (system-architect)`. Then, if a task was given, proceed with it as system-architect would — spawn that subagent (or apply that skill) so the user is not left at a dead end.

Task: $ARGUMENTS
