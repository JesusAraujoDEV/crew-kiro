---
description: "Retired alias — use /crew:ops (platform)"
argument-hint: <task>
---

This alias was retired in the catalog consolidation (25 → 17 roles). performance-reliability merged into the unified platform role: everything post-merge is OPS.

Tell the user in one line: `PERF → use /crew:ops (platform)`. Then, if a task was given, proceed with it as platform would — spawn that subagent (or apply that skill) so the user is not left at a dead end.

Task: $ARGUMENTS
